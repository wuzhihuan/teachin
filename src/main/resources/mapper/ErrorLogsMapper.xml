<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dizhejiang.teachin.mapper.ErrorLogsMapper">

    <insert id="save">
        insert into t_error_logs (error_info,create_time,user_id)
        values
         (#{errorLogs.errorInfo},#{errorLogs.createTime},#{errorLogs.userId})
    </insert>
</mapper>