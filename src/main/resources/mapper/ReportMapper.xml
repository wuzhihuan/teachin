<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dizhejiang.teachin.mapper.ReportMapper">
    <insert id="save">
        insert into t_Report (teachin_id,type,user_id,content)
        values
               (#{report.teachinId},#{report.type},#{report.userId},#{report.content})
    </insert>
</mapper>